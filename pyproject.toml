[project]
name = "agario-rl-experiment"
version = "0.1.0"
description = "Multi-agent Agar.io-style RL lab with a browser game runtime."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
  "numpy>=1.26",
  "fastapi>=0.115",
  "torch>=2.2",
  "PyYAML>=6.0",
  "matplotlib>=3.8",
  "uvicorn[standard]>=0.30",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
license-files = ["LICENSE"]

[tool.setuptools.packages.find]
include = ["agario_rl*"]
